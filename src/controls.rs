struct Controls {
    are_enabled: bool,
    forward: f32,
    backward: f32,
    left: f32,
    right: f32,
    //Are we in, space/water?
    up: f32,
    down: f32
}

impl Controls {
    
}